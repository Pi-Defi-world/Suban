import React from 'react';
import ReactDOM from 'react-dom/client';
import { EscrowViewer } from './EscrowViewer';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EscrowViewer />
  </React.StrictMode>
);
