[package]
name = "event-registry"
version.workspace = true
edition.workspace = true
description = "Event Registry — unified cross-primitive event storage and querying."
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
