[package]
name = "usdc-vault"
version.workspace = true
edition.workspace = true
description = "USDC bridge vault for real Stellar USDC Asset Contract integration."
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
