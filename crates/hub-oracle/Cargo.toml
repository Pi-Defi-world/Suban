[package]
name = "hub-oracle"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }
hub-types = { path = "../hub-types" }
hub-errors = { path = "../hub-errors" }
